[toolchain]
channel = "1.94"
components = ["rustfmt", "clippy"]
# The Leptos frontend half of `authenc-web` is compiled to wasm; without this
# target `cargo leptos build` fails with a confusing "can't find crate" error.
targets = ["wasm32-unknown-unknown"]
profile = "minimal"

[workspace]
resolver = "2"
members = ["crates/*"]

[workspace.package]
version = "0.4.0"
edition = "2024"
rust-version = "1.90"
authors = ["Authenc Team <security@authenc.dev>"]
license = "Apache-2.0"
description = "Enterprise-grade Identity and Access Management (IAM) service"
repository = "https://github.com/authenc/authenc"
homepage = "https://authenc.dev"
documentation = "https://docs.authenc.dev"
keywords = ["iam", "authentication", "authorization", "security", "rust"]
categories = ["authentication", "cryptography", "web-programming"]

[workspace.dependencies]
# Core async runtime
anyhow = "1.0.100"
tokio = { version = "1.47.1", features = ["rt-multi-thread", "macros", "full"] }
futures = "0.3.31"
futures-util = "0.3.31"
tokio-util = { version = "0.7.13", features = ["full"] }
async-trait = "0.1.85"
pin-project = "1.1.8"

# HTTP and web frameworks
http = "1.2.0"
http-body = "1.0.1"
http-body-util = "0.1.2"
temp-env = "0.3.6"

# Web Frameworks and HTTP
axum = { version = "0.8.1", features = ["http1", "http2", "json", "macros"] }
axum-extra = { version = "0.10.0", features = ["cookie"] }
tower = { version = "0.5.2", features = ["util", "timeout", "load-shed", "limit"] }
tower-http = { version = "0.6.2", features = ["cors", "trace", "compression-br", "add-extension"] }
hyper = { version = "1.5.1", features = ["full"] }
reqwest = { version = "0.12.12", features = ["json"] }

# Authentication and Security
jsonwebtoken = { version = "10.0", default-features = false, features = ["aws_lc_rs"] }
argon2 = { version = "0.5", features = ["std", "rand"] }
bcrypt = "0.17.1"

# Database
tokio-postgres = { version = "0.7.14", features = ["with-serde_json-1", "with-uuid-1", "with-time-0_3", "with-chrono-0_4"] }
deadpool-postgres = { version = "0.14.1", features = ["serde"] }
redis = { version = "0.24", features = ["tokio-comp"] }
deadpool-redis = { version = "0.14" }

# Serialization & Data
uuid = { version = "1.11.1", features = ["v4", "serde", "js"] }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.140"
thiserror = "2.0.17"
once_cell = "1.20.2"

# Logging and Tracing
tracing = "0.1.41"
tracing-subscriber = { version = "0.3.19", features = ["env-filter", "fmt", "json"] }
tracing-opentelemetry = "0.32.0"
opentelemetry = "0.27.0"
opentelemetry-otlp = "0.27.0"
log = "0.4.27"
env_logger = "0.11.7"

# Cryptography
rand = "0.8"
rand_chacha = "0.3"
getrandom = "0.2"
aes-gcm = "0.11.0-rc.2"
base64 = "0.22"
base64ct = "1.8"
ed25519-dalek = { version = "2.1", features = ["rand_core", "serde"] }
p256 = { version = "0.13.2", features = ["ecdsa"] }
p384 = { version = "0.13.0", features = ["ecdsa"] }
p521 = { version = "0.13.3", features = ["ecdsa"] }
ecdsa = { version = "0.16.9" }
ed448-goldilocks = "0.9.0"
x509-parser = "0.15"
sha2 = "0.10"
hex = "0.4"

# Time & Concurrency
chrono = { version = "0.4", features = ["serde"] }
dashmap = { version = "6.1.0", features = ["raw-api"] }
num_cpus = "1.16"
serde_qs = "0.15.0"
url = { version = "2.0" }
urlencoding = "2.1"

# Messaging
rdkafka = { version = "0.38", features = ["tokio"] }

# TOTP
totp-rs = { version = "5.7", default-features = false }
base32 = "0.4"
hmac = "0.12"
sha1 = "0.10"

# LDAP
ldap3 = "0.12.1"

# SAML XML Processing
quick-xml = { version = "0.31", features = ["serialize"] }
flate2 = { version = "1.0", features = ["zlib"] }

# Kubernetes Operator
kube = { version = "0.87", features = ["runtime", "derive"] }
k8s-openapi = { version = "0.20", features = ["v1_28"] }
schemars = "0.8"
tokio-stream = "0.1"

# TLS & Security (Pure Rust)
rustls = { version = "0.23" }
rustls-pemfile = "2.2"
rustls-native-certs = "0.8"
webpki-roots = "1.0"
ring = "0.17"
regex = "1.0"

# OpenSSL
openssl = "0.10"

# Hardware Security Module
tss-esapi = { version = "7.4" }

# Post-Quantum Cryptography
pqcrypto-mldsa = { version = "0.1.2" }
pqcrypto-mlkem = { version = "0.1.1" }
pqcrypto-falcon = { version = "0.4.1" }
pqcrypto-traits = "0.3.5"

# WebAuthn
webauthn-rs = { version = "0.5" }

# Shamir Secret Sharing dependencies
curve25519-dalek = "4.1"
subtle = "2.6"
zeroize = { version = "1.8", features = ["zeroize_derive"] }
bincode = "1.3"
hkdf = "0.12"

# Metrics
metrics = { version = "0.21" }
metrics-exporter-prometheus = { version = "0.12", default-features = false, features = ["http-listener"] }
fluent-bundle = "0.16.0"
unic-langid = "0.9.6"
axum-test = "18.1"

# Dev dependencies
tempfile = "3"
tokio-test = "0.4"
serial_test = "3.2"
lazy_static = "1.5"

# Performance optimizations
[profile.dev]
opt-level = 1
debug = true
incremental = true
codegen-units = 256
lto = false
split-debuginfo = "unpacked"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
overflow-checks = false
debug = false
incremental = false

[profile.dev.package."*"]
opt-level = 2

[profile.dev.package.tokio]
opt-level = 3

[profile.dev.package.axum]
opt-level = 3
